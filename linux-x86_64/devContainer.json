{
  "name": "Linux x86_64 (amd64)",
  "build": {
    "dockerfile": "Dockerfile",
    "context": "..",
    "options": ["--platform=linux/amd64"]
  },
  "postCreateCommand": "echo '✅ Ready for x86_64 development'",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-vscode.cpptools"
      ]
    }
  },
  "features": {
    // Add any official devcontainer features here if needed (e.g. git, python, etc.)
  }
}